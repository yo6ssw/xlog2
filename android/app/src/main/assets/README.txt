Drop the AD1C cty.dat (Big CTY) here to enable offline DXCC/zone lookups.
Without it, DXCC indicators stay blank (the feature degrades gracefully).
Same file the desktop uses: ~/.local/share/xlog2/cty.dat
