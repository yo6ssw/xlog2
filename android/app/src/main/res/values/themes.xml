<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- A Material3 DayNight base; the actual colors come from Compose's
         XlogTheme. This exists only so the manifest android:theme resolves and
         the splash/system bars look right before Compose takes over. -->
    <style name="Theme.Xlog2" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
